#FROM python:3.12-slim

# Рабочая директория
WORKDIR /app

# Копируем только зависимости (если есть) — тут их нет
COPY ./app.py /app/

# Открываем порт для документации (не публикуем наружу)
EXPOSE 8080

# Команда запуска
CMD ["python3", "app.py"]
